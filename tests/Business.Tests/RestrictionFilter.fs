module RestrictionFilterTest

open Expecto
open Business

let tests = 
    testList "Restriction filter tests" [ 
        
        test "filter must return true when business forms are equal" {
            let filterResult = Restrictions.filter

        }
    ]